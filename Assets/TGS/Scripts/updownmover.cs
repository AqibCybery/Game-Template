using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class updownmover : MonoBehaviour {
	public float points;
	public float delayspeed;
	public string axis;
	// Use this for initialization
	void Start () {
//		iTween.MoveBy(gameObject, iTween.Hash(axis, points, "easeType", "easeInOutExpo", "loopType", "pingPong", "delay", -1,"Time",delayspeed));

	}
	
	// Update is called once per frame

}
